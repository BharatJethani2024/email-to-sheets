Screenshots
Add the following images to this folder and they will display in the main README and docs:

scenario-canvas.png — the full three-module scenario (Gmail → Text parser → Google Sheets) with the green success checkmarks visible
text-parser-config.png — the Text Parser module open, showing the pattern and Global match = Yes
parser-output.png — the parser output bundle showing extracted shop/sales/customers/date values
spreadsheet-result.png — the Google Sheet with several logged rows
Tips for clean screenshots:

Crop out anything personal (email addresses, account names)
Use the same two-shop example email so the story is consistent across images
A short screen-recording GIF of one run is a strong addition if you want one
